//const nombres = ['ana', 'javier', 'maru', 'luis'];

//nombres.splice(index, cantidad) indice y cantidad de elemenos a eliminar o agregar

//nombres.splice(2,0, 'jose'); // en la posicion 2 no elimino a nadie y agrego a jose 

//nombres.splice(2, 1, 'jose'); //en la posicion 2 elimino a 1 (maru) y agrego a jose

//nombres.splice(2, 2, 'jose', 'toto'); //en el indice 2 elimino 2 y reemplazo por jose y toto

//nombres.splice(2, 2);  // en el indice 2 elimine a 2  // ana, javier 
//console.log(nombres)